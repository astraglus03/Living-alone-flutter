import 'package:flutter/material.dart';

class NeighborCommunicationScreen extends StatelessWidget {
  const NeighborCommunicationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(
          height: 80,
          width: 80,
          color: Colors.red,
        ),
      ),
    );
  }
}
